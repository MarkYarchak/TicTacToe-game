$lineItemsCount = 3;

.game-board {
  display: grid;
  grid-template-rows: repeat($lineItemsCount, 100px);
  grid-template-columns: repeat($lineItemsCount, 100px);
  grid-row-gap: 8px;
  grid-column-gap: 8px;
  background-color: black;
  margin-top: -50px;
}
